apiVersion: v1
kind: Service
metadata:
  name: sample-node-app
  namespace: demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
  selector:
    app: sample-node-app
